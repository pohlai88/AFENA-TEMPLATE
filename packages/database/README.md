<!-- AUTOGEN:START -->
<!-- Generated by afena readme gen — DO NOT EDIT inside this block -->
<!-- Signature: sha256:ee2a65673d8583e1f36e85adef3d386d92b554132f6f2f1af8f49ee7d19be0e3 -->
# afena-database

## Package Overview

📁 4 Subdirectories

### What's Inside

- API clients
- Configuration
- Helper functions
- Main exports
- Schemas
- Type definitions

### Directory Structure

```
src/
├── governance/
├── helpers/
├── schema/
└── scripts/
```

### Entry Points

- `src/index.ts`
- `src/schema/index.ts`

## Usage

```ts
import { /* ... */ } from 'afena-database';
```

## API

See source files for available exports.

## Installation

```bash
pnpm -w add afena-database
```

## Exports

| Subpath | Conditions |
|---|---|
| `.` | default: `./src/index.ts` |

## Scripts

| Script | Command |
|---|---|
| `build` | `tsup` |
| `db:barrel` | `npx tsx src/scripts/generate-schema-barrel.ts && npx tsx src/scripts/generate-table-registry.ts` |
| `db:drift-check` | `npx tsx src/scripts/drift-check.ts` |
| `db:generate` | `drizzle-kit generate` |
| `db:lint` | `npx tsx src/scripts/schema-lint.ts` |
| `db:migrate` | `drizzle-kit migrate` |
| `db:push` | `drizzle-kit push` |
| `db:studio` | `drizzle-kit studio` |
| `dev` | `tsup --watch` |
| `lint` | `eslint . --ext .js,.jsx,.ts,.tsx --cache --concurrency=auto` |
| `lint:ci` | `eslint . --ext .js,.jsx,.ts,.tsx --rule 'import/no-cycle: error'` |
| `lint:fix` | `eslint . --ext .js,.jsx,.ts,.tsx --cache --fix` |
| `type-check` | `tsc --noEmit` |

## Architecture

```
src/
└── index.ts    # Public API
```

## Dependencies

| Package | Version |
|---|---|
| `@neondatabase/serverless` | `^1.0.0` |
| `drizzle-orm` | `^0.44.0` |

## Related Packages

- `afena-eslint-config`
- `afena-typescript-config`

## Contributing

### Development Workflow

1. Make changes to source files
2. Run `pnpm lint:fix` to fix linting issues
3. Run `pnpm type-check` to verify types
4. Run `pnpm test` to verify tests pass
5. Run `pnpm build` to verify build succeeds

### Code Quality

- All code must pass ESLint checks
- All code must pass TypeScript type checking
- All tests must pass
- Maintain or improve test coverage

<!-- AUTOGEN:END -->
