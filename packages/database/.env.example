# Direct TCP connection — required for migrations and DDL
DATABASE_URL_MIGRATIONS=postgresql://user:pass@ep-xxx.ap-southeast-1.aws.neon.tech/neondb?sslmode=require

# Pooled connection — used by Drizzle ORM at runtime (read-write)
DATABASE_URL=postgresql://user:pass@ep-xxx-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require

# Read-only replica — used by dbRo for list pages, dashboards, search (optional, falls back to DATABASE_URL)
# DATABASE_URL_RO=postgresql://user:pass@ep-xxx-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
