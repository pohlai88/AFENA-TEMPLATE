<!-- AUTOGEN:START -->
<!-- Generated by afena readme gen — DO NOT EDIT inside this block -->
<!-- Signature: sha256:0ceeb7400f8b3f5003420c36615d0d037049768db8b23d7b1fc64fc00900bb8e -->
# afena-ui

## Package Overview

📁 4 Subdirectories

### What's Inside

- Configuration
- Main exports
- React components
- React hooks
- Type definitions
- Utility functions

### Directory Structure

```
src/
├── components/
├── hooks/
├── lib/
└── providers/
```

### Entry Points

- `src/index.ts`
- `src/providers/index.ts`

## Usage

```tsx
import { ComponentsAccordion } from 'afena-ui';
```

## Components

- **ComponentsAccordion**
- **ComponentsAlertDialog**
- **ComponentsAlert**
- **ComponentsAnimateOnScroll**
- **ComponentsAspectRatio**
- **ComponentsAvatar**
- **ComponentsBadge**
- **ComponentsBreadcrumb**
- **ComponentsButtonGroup**
- **ComponentsButton**
- **ComponentsCalendar**
- **ComponentsCard**
- **ComponentsCarousel**
- **ComponentsChart**
- **ComponentsCheckbox**
- **ComponentsCollapsible**
- **ComponentsCombobox**
- **ComponentsCommand**
- **ComponentsContextMenu**
- **ComponentsDataTable**
- **ComponentsDatePicker**
- **ComponentsDialog**
- **ComponentsDirection**
- **ComponentsDrawer**
- **ComponentsDropdownMenu**
- **ComponentsEmpty**
- **ComponentsField**
- **ComponentsFileUpload**
- **ComponentsForm**
- **ComponentsHoverCard**
- **ComponentsInputGroup**
- **ComponentsInputOtp**
- **ComponentsInput**
- **ComponentsItem**
- **ComponentsKbd**
- **ComponentsLabel**
- **ComponentsMenubar**
- **ComponentsNativeSelect**
- **ComponentsNavigationMenu**
- **ComponentsPagination**
- **ComponentsPopover**
- **ComponentsProgress**
- **ComponentsRadioGroup**
- **ComponentsResizable**
- **ComponentsScrollArea**
- **ComponentsSelect**
- **ComponentsSeparator**
- **ComponentsSheet**
- **ComponentsSidebar**
- **ComponentsSkeleton**
- **ComponentsSlider**
- **ComponentsSonner**
- **ComponentsSpinner**
- **ComponentsSwitch**
- **ComponentsTable**
- **ComponentsTabs**
- **ComponentsTextarea**
- **ComponentsToggleGroup**
- **ComponentsToggle**
- **ComponentsTooltip**
- **ComponentsTypography**
- **ProvidersThemeProvider**
- **ProvidersToasterProvider**
- **ProvidersTooltipProvider**

## Installation

```bash
pnpm -w add afena-ui
```

### Peer Dependencies

This package requires the following peer dependencies:

- `react`
- `react-dom`
- `tailwindcss`

## Exports

| Subpath | Conditions |
|---|---|
| `.` | import: `./dist/index.mjs`, require: `./dist/index.js`, types: `./dist/index.d.ts` |
| `./components/*` | default: `./src/components/*.tsx` |
| `./hooks/*` | default: `./src/hooks/*.ts` |
| `./lib/*` | default: `./src/lib/*.ts` |
| `./providers` | default: `./src/providers/index.tsx` |
| `./providers/*` | default: `./src/providers/*.tsx` |
| `./styles/*` | default: `./src/styles/*.css` |

## Scripts

| Script | Command |
|---|---|
| `build` | `tsup` |
| `dev` | `tsup --watch` |
| `engine:generate` | `tsx engine/generate.ts` |
| `lint` | `eslint . --ext .js,.jsx,.ts,.tsx --cache --concurrency=auto` |
| `lint:ci` | `eslint . --ext .js,.jsx,.ts,.tsx --rule 'import/no-cycle: error'` |
| `lint:fix` | `eslint . --ext .js,.jsx,.ts,.tsx --cache --fix` |
| `prebuild` | `tsx engine/generate.ts` |
| `type-check` | `tsc --noEmit` |

## Architecture

## Dependencies

| Package | Version |
|---|---|
| `@base-ui/react` | `^1.1.0` |
| `@hookform/resolvers` | `^5.2.2` |
| `@tanstack/react-table` | `^8.21.3` |
| `class-variance-authority` | `catalog:` |
| `clsx` | `catalog:` |
| `cmdk` | `^1.1.1` |
| `date-fns` | `^4.1.0` |
| `embla-carousel-react` | `^8.6.0` |
| `input-otp` | `^1.4.2` |
| `lucide-react` | `catalog:` |
| `next-themes` | `^0.4.6` |
| `radix-ui` | `^1.4.3` |
| `react` | `catalog:` |
| `react-day-picker` | `^9.13.2` |
| `react-dom` | `catalog:` |
| `react-hook-form` | `^7.71.1` |
| `react-resizable-panels` | `^4` |
| `recharts` | `2.15.4` |
| `sonner` | `^2.0.7` |
| `tailwind-merge` | `catalog:` |
| `tw-animate-css` | `^1.4.0` |
| `vaul` | `^1.1.2` |

## Peer Dependencies

| Package | Version |
|---|---|
| `react` | `^19.2.3` |
| `react-dom` | `^19.2.3` |
| `tailwindcss` | `>=4.0.0` |

## Related Packages

- `afena-eslint-config`
- `afena-typescript-config`

## Contributing

### Development Workflow

1. Make changes to source files
2. Run `pnpm lint:fix` to fix linting issues
3. Run `pnpm type-check` to verify types

### Code Quality

- All code must pass ESLint checks
- All code must pass TypeScript type checking

<!-- AUTOGEN:END -->

---

## shadcn/ui Integration

This package uses [shadcn/ui](https://ui.shadcn.com) as the component primitive layer, with the Afena Tailwind Engine providing the design tokens (colors, spacing, typography, etc.).

### Architecture

```
Layer 1: Tailwind Engine (DNA)
  └─ tailwindengine.json → engine/generate.ts → CSS tokens (@theme)

Layer 2: Bridge (src/styles/globals.css)
  └─ Maps engine tokens → shadcn semantic variables (--primary, --border, etc.)

Layer 3: shadcn/ui Components (Organs)
  └─ Accessible, headless primitives styled via semantic variables
```

### Directory Structure

```
packages/ui/src/
├── components/       ← shadcn primitives (button, card, dialog, etc.)
├── hooks/            ← shadcn hooks (use-mobile, etc.)
├── lib/
│   └── utils.ts      ← cn() utility
├── styles/
│   └── globals.css   ← Bridge: engine tokens → shadcn semantic vars
└── index.ts          ← Barrel exports
```

### Adding New Components

From the monorepo root:

```bash
# Add a single component
pnpm ui:add accordion

# Add multiple components
pnpm ui:add accordion alert-dialog avatar

# Or run directly from packages/ui
pnpm dlx shadcn@latest add accordion
```

The CLI installs components into `src/components/`, adds Radix dependencies to `package.json`, and resolves imports automatically.

**After adding**, update the barrel export in `src/index.ts`:

```ts
export * from './components/accordion';
```

### Updating Components (Sync from Upstream)

```bash
# Check what changed upstream
pnpm ui:diff

# Overwrite with latest upstream version (review diff first!)
pnpm ui:update accordion
```

### Available Components (Installed)

| Component      | File                | Radix Primitive |
| -------------- | ------------------- | --------------- |
| Button         | `button.tsx`        | `Slot`          |
| Card           | `card.tsx`          | —               |
| Dialog         | `dialog.tsx`        | `Dialog`        |
| Dropdown Menu  | `dropdown-menu.tsx` | `DropdownMenu`  |
| Input          | `input.tsx`         | —               |
| Label          | `label.tsx`         | `Label`         |
| Select         | `select.tsx`        | `Select`        |
| Sonner (Toast) | `sonner.tsx`        | — (sonner)      |
| Tabs           | `tabs.tsx`          | `Tabs`          |
| Tooltip        | `tooltip.tsx`       | `Tooltip`       |

### Remaining Components (Install on Demand)

Accordion, Alert, Alert Dialog, Aspect Ratio, Avatar, Badge, Breadcrumb, Calendar, Carousel, Chart, Checkbox, Collapsible, Combobox, Command, Context Menu, Data Table, Date Picker, Direction, Drawer, Empty, Field, Hover Card, Input Group, Input OTP, Item, Kbd, Menubar, Native Select, Navigation Menu, Pagination, Popover, Progress, Radio Group, Resizable, Scroll Area, Separator, Sheet, Sidebar, Skeleton, Slider, Spinner, Switch, Table, Textarea, Toggle, Toggle Group, Typography

### Key Principles

- **Open Code** — shadcn components are copied into your codebase, not imported from `node_modules`. You own them.
- **Modify Freely** — Edit component files directly to match your design system.
- **JIT Install** — Only add components when you need them to keep the bundle clean.
- **Engine First** — All colors/tokens come from the Tailwind Engine. The bridge layer maps them to shadcn's expected CSS variables.
