/**
 * loadCurrent() — Phase 4 Stub
 *
 * Will extract the pre-transaction entity fetch from mutate() into a pure
 * Plan-phase function. Used for update/delete/restore verbs.
 *
 * Returns the current row or null (for create).
 *
 * See: INTEGRATION_PLAN.md Phase 4 — Thin Orchestrator
 */
export function loadCurrent() {
  throw new Error('loadCurrent() is not yet implemented — Phase 4 stub');
}
