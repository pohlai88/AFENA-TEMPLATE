/**
 * buildIntents() — Phase 3 Stub
 *
 * Will build OutboxIntents from the MutationPlan after entity write.
 * Custom handlers can add their own intents via planXxx() hooks.
 *
 * See: INTEGRATION_PLAN.md Phase 3 — MutationPlan + Outbox
 */
export function buildIntents() {
  throw new Error('buildIntents() is not yet implemented — Phase 3 stub');
}
