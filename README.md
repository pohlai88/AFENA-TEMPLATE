<!-- AUTOGEN:START -->
<!-- Generated by afena readme gen — DO NOT EDIT inside this block -->
<!-- Signature: sha256:f3dd38a639bcd6b07ebcaf8b6c277a81472eb6f574f51f6a45e4c2e4509d6103 -->
# afena-monorepo
## Structure

```
afena-monorepo/
├── apps/
│   ├── web/
├── packages/
│   ├── advisory/
│   ├── canon/
│   ├── crud/
│   ├── database/
│   ├── eslint-config/
│   ├── logger/
│   ├── migration/
│   ├── search/
│   ├── typescript-config/
│   ├── ui/
│   ├── vitest-config/
│   └── workflow/
├── tools/
│   └── afena-cli/
├── turbo.json
├── pnpm-workspace.yaml
└── package.json
```

## Packages

| Package | Path | Type | Description |
|---|---|---|---|
| `web` | `apps/web` | app |  |
| `afena-advisory` | `packages/advisory` | library |  |
| `afena-canon` | `packages/canon` | library |  |
| `afena-crud` | `packages/crud` | library |  |
| `afena-database` | `packages/database` | library |  |
| `afena-eslint-config` | `packages/eslint-config` | config |  |
| `afena-logger` | `packages/logger` | library |  |
| `afena-migration` | `packages/migration` | library |  |
| `afena-search` | `packages/search` | library |  |
| `afena-typescript-config` | `packages/typescript-config` | config |  |
| `afena-ui` | `packages/ui` | ui |  |
| `afena-vitest-config` | `packages/vitest-config` | config |  |
| `afena-workflow` | `packages/workflow` | library |  |
| `@afena/cli` | `tools/afena-cli` | tool |  |

## Scripts

| Script | Command |
|---|---|
| `pnpm afena` | `node tools/afena-cli/dist/index.js` |
| `pnpm afena:dev` | `tsx tools/afena-cli/src/index.ts` |
| `pnpm afena:discover` | `node tools/afena-cli/dist/discover.js` |
| `pnpm audit` | `pnpm audit --audit-level=moderate` |
| `pnpm audit:fix` | `pnpm audit --fix` |
| `pnpm audit:prod` | `pnpm audit --prod --audit-level=moderate` |
| `pnpm build` | `turbo run build` |
| `pnpm check:no-console` | `node -e "const{execSync}=require('child_process');try{const r=execSync('npx rg console\\\\. --type ts --type tsx -l --glob !node_modules --glob !dist --glob !.next --glob !**/client-logger.ts --glob !**/*.config.* --glob !tools/**',{encoding:'utf8'});if(r.trim()){console.error('INVARIANT-08 violation: console.* found in:\n'+r);process.exit(1)}}catch(e){if(e.status===1)process.exit(0);throw e}"` |
| `pnpm ci:invariants` | `node tools/ci-invariants.mjs` |
| `pnpm clean` | `turbo run clean` |
| `pnpm dev` | `turbo run dev` |
| `pnpm engine:generate` | `pnpm --filter afena-ui engine:generate` |
| `pnpm format` | `prettier --write "**/*.{ts,tsx,md}"` |
| `pnpm format:check` | `prettier --check "**/*.{ts,tsx,md}"` |
| `pnpm lint` | `turbo run lint` |
| `pnpm lint:ci` | `turbo run lint:ci` |
| `pnpm lint:fix` | `turbo run lint:fix` |
| `pnpm postinstall` | `node -e "try{require('./tools/afena-cli/dist/discover.js')}catch(e){process.exit(0)}"` |
| `pnpm sbom` | `pnpm dlx @cyclonedx/cdxgen -r --no-install-deps -o sbom.json` |
| `pnpm test` | `vitest run` |
| `pnpm test:coverage` | `vitest run --coverage` |
| `pnpm test:e2e` | `playwright test --project=chromium` |
| `pnpm test:e2e:all` | `playwright test` |
| `pnpm test:e2e:codegen` | `playwright codegen http://localhost:3000` |
| `pnpm test:e2e:debug` | `playwright test --debug` |
| `pnpm test:e2e:install` | `playwright install --with-deps` |
| `pnpm test:e2e:report` | `playwright show-report` |
| `pnpm test:e2e:ui` | `playwright test --ui` |
| `pnpm test:profile` | `node scripts/profile-vitest.js` |
| `pnpm test:profile:coverage` | `node scripts/profile-vitest.js coverage` |
| `pnpm test:profile:main` | `node scripts/profile-vitest.js main` |
| `pnpm test:profile:runner` | `node scripts/profile-vitest.js runner` |
| `pnpm test:ui` | `vitest --ui` |
| `pnpm test:watch` | `vitest` |
| `pnpm type-check` | `turbo run type-check` |
| `pnpm type-check:strict` | `turbo run type-check -- --strict` |
| `pnpm ui:add` | `pnpm dlx shadcn@latest add -c apps/web` |
| `pnpm ui:diff` | `pnpm dlx shadcn@latest diff -c apps/web` |
| `pnpm ui:update` | `pnpm dlx shadcn@latest add -o -c apps/web` |

<!-- AUTOGEN:END -->

A modern monorepo powered by Turborepo, Next.js 16 with Turbopack, and TypeScript.

## Getting Started

1. Install dependencies:

   ```bash
   pnpm install
   ```

2. Start the development server:

   ```bash
   pnpm dev
   ```

3. Open [http://localhost:3000](http://localhost:3000) to view the application.

## Development Workflow

1. Make changes to packages or apps
2. Turborepo automatically handles dependencies and caching
3. Use `pnpm build` to verify changes
4. Use `pnpm lint` and `pnpm type-check` to ensure code quality

**Lint & type debugging:** Cursor skill at `.cursor/skills/lint-types-debug/` — use when lint or type-check fails, or when debugging INVARIANT/EX-LINT issues.

## Adding New Packages

1. Create a new directory in `packages/` or `tools/`
2. Add a `package.json` with the appropriate name
3. Configure TypeScript to extend from `afena-typescript-config`
4. Run `pnpm afena readme gen` to generate the README

## Deployment

The monorepo is configured for optimal deployment with Vercel. Each app can be deployed independently.

## Learn More

- [Turborepo Documentation](https://turbo.build/repo/docs)
- [Next.js Documentation](https://nextjs.org/docs)
- [PNPM Workspace Documentation](https://pnpm.io/workspaces)
