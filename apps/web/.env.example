# ============================================================================
# Afenda — Next.js App Environment (example)
# Copy to .env and fill in values.
# ============================================================================

# ── Application ──────────────────────────────────────────────────────────────
NODE_ENV=development
LOG_LEVEL=debug
PORT=3000
BASE_URL=http://localhost:3000

# ── GAP-DB-004: Search Outbox + Drain ────────────────────────────────────────
# Required for /api/internal/search/drain and /api/internal/search/bootstrap
# Generate: openssl rand -hex 32
CRON_SECRET=

# ── Neon Database ────────────────────────────────────────────────────────────
DATABASE_URL=
DATABASE_URL_RO=
DATABASE_URL_MIGRATIONS=
NEON_PROJECT_ID=
NEON_API_KEY=

# ── Neon Auth ────────────────────────────────────────────────────────────────
NEON_AUTH_BASE_URL=
NEON_AUTH_COOKIE_SECRET=
NEXT_PUBLIC_NEON_AUTH_URL=
JWKS_URL=
NEON_JWT_SECRET=

# ── OAuth ────────────────────────────────────────────────────────────────────
GITHUB_ID=
GITHUB_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ── Phase 2C: Optional list cache (Upstash Redis) ─────────────────────────────
# When set, listEntities results are cached (TTL 60s). orgId required. Defer until list latency is a bottleneck.
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
# LIST_CACHE_TTL_SEC=60

# ── Storage (R2) ─────────────────────────────────────────────────────────────
STORAGE_PROVIDER=r2
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_PUBLIC_BASE_URL=
