/**
 * Entity config registry — re-exports from generated.
 * Plan Phase 4: generated/ + overrides/ + registry.
 *
 * Overrides: Place <entityType>.form.ts or <entityType>.columns.ts in overrides/
 * to override generated config. Merge logic can be added when overrides exist.
 * 
 * NOTE: Generated configs archived to ARCHIVE/erpnext-adapter-20260217
 */

// Empty registry - generated configs were archived
export const FORM_CONFIGS = {} as const;
export const COLUMNS_CONFIGS = {} as const;

export type FormConfigEntityType = never;
export type ColumnsConfigEntityType = never;
