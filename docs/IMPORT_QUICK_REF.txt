┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│  📦 AFENDA-NEXUS Import Patterns Quick Reference                   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ 1️⃣  CROSS-PACKAGE IMPORTS → Use Package Names                      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ✅ CORRECT:                                                        │
│  import { createLogger } from 'afenda-logger';                      │
│  import { db, sql } from 'afenda-database';                         │
│  import type { DocStatus } from 'afenda-canon';                     │
│                                                                     │
│  ❌ WRONG:                                                          │
│  import { createLogger } from '@/logger';                           │
│  import { db } from '../../database/src';                           │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ 2️⃣  INTERNAL PACKAGE IMPORTS → Use Relative Paths                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ✅ CORRECT (in packages/crud/src/mutate.ts):                       │
│  import { buildContext } from './context';                          │
│  import { validateMutation } from './validators';                   │
│  import type { MutationOpts } from '../types';                      │
│                                                                     │
│  ❌ WRONG:                                                          │
│  import { buildContext } from 'afenda-crud/context';                │
│  import { validateMutation } from '@/validators';                   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ 3️⃣  APP-LEVEL IMPORTS → Use @ Alias                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ✅ CORRECT (in apps/web/app/**):                                   │
│  import { auth } from '@/lib/auth/server';                          │
│  import { Button } from '@/components/ui/button';                   │
│  import { getUser } from '@/app/actions/user';                      │
│                                                                     │
│  ❌ WRONG:                                                          │
│  import { auth } from '../../../src/lib/auth/server';               │
│  import { Button } from './../../components/ui/button';             │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ 4️⃣  SHARED UI IMPORTS → Use Package Name                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ✅ CORRECT:                                                        │
│  import { Button, Card } from '@afenda/ui';                         │
│  import { cn } from '@afenda/ui/lib/utils';                         │
│                                                                     │
│  ❌ WRONG:                                                          │
│  import { Button } from '../../packages/ui/src/components/button';  │
│  import { cn } from '@/ui/lib/utils';  // (unless in app context)   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ 📋 IMPORT ORDER                                                     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  1. Node.js built-ins          import { readFile } from 'node:fs'  │
│  2. External packages           import { z } from 'zod'             │
│  3. Cross-package (afenda-*)    import { db } from 'afenda-database'│
│  4. App aliases (@/)            import { auth } from '@/lib/auth'   │
│  5. Parent imports (../)        import { utils } from '../utils'    │
│  6. Sibling imports (./)        import { helper } from './helper'   │
│  7. Type imports                import type { User } from './types' │
│                                                                     │
│  ⚠️  Add blank lines between groups!                                │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ 🎯 LOCATION-BASED EXAMPLES                                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  📁 packages/workflow/src/engine.ts                                 │
│  ├─ import { db } from 'afenda-database';        // Cross-package   │
│  ├─ import { createLogger } from 'afenda-logger'; // Cross-package  │
│  ├─ import { executeNode } from './executor';     // Internal       │
│  └─ import type { WorkflowCtx } from './types';   // Internal       │
│                                                                     │
│  📁 apps/web/app/dashboard/page.tsx                                 │
│  ├─ import { Button } from '@afenda/ui';          // Shared package │
│  ├─ import { getUser } from '@/app/actions/user'; // App action     │
│  ├─ import { auth } from '@/lib/auth/server';     // App lib        │
│  └─ import type { UserData } from '@/lib/types';  // App type       │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ ✅ VALIDATION COMMANDS                                              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  pnpm type-check     # Verify TypeScript compiles correctly         │
│  pnpm lint           # Check import patterns with ESLint            │
│  pnpm build          # Ensure project references work               │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ 📚 FULL DOCUMENTATION                                               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  See: docs/IMPORT_ALIAS_GUIDE.md                                    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
